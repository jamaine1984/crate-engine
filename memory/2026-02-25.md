# 2026-02-25 Session Notes

## v149 — FPS Viewmodel + Unity/Unreal Export
- Added FPS weapon viewmodel system in character.mjs: separate THREE.Scene rendered on top (clearDepth), weapon bob/sway/recoil/ADS centering
- `_fpScene`, `_fpCamera`, `_fpWeaponGroup` — separate render pass via `renderFPWeapon(renderer)`
- `_setupFPWeapon()` clones equipped weapon mesh, called on camera toggle + weapon swap
- Recoil triggers on attack (1.0 light, 1.5 heavy)
- Export to Unity + Unreal: GLTFExporter → GLB download + import guide markdown
- Two new buttons in bottom bar (green Unity, blue Unreal), NL commands `export unity`/`export unreal`
- Tab/I key bound to `toggleInventoryPanel()` in engine.mjs keydown handler
- Deployed to Cloudflare Pages, commit 6770ea8

## Version State
- engine.mjs v149, character.mjs v79, index.html v=149
- character.mjs import: `./character.mjs?v=79`

## Kohari's Vision
- Users build games in <5 minutes with natural language
- Export to Unity/Unreal for better graphics
- Sell engine for $100M+ or build community
- FPS must show arms + two-handed weapon holding
