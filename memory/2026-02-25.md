# 2026-02-25 Session Notes

## Crate Engine v106-v117

### Weapon System Overhaul
- **Two-handed IK** implemented: `_applyTwoHandedIK()` runs after animation mixer, left hand tracks weapon grip (swords: hilt, guns: foregrip), lerp 0.4
- **Holster system working**: equip → weapon on back, keys 1/2/3 draw/sheathe toggle
- **Weapon scale fix**: bone world scale is 32x for knight — must compensate. Formula: `localScale = desiredWorld / (geoSize * boneWorldScale)`
- **Back-mount uses model-relative positioning** (not bone-local) — more predictable
- **Gun scale reduced**: ranged 0.5 vs melee 0.9 world units
- **v117: Real GLB weapon models** replace procedural primitives — sword_iron.glb, axe_iron.glb, dagger_00.glb, hammer_00.glb, spear_00.glb, bow_wood.glb, blasterb-h.glb for guns. Async load with procedural fallback.

### Death Screen Bug Fix (CRITICAL)
- `_playerDeath()` was creating new overlay every call — resulted in 40+ stacked death screens
- Fixed: `querySelectorAll('#death-screen').forEach(d => d.remove())` before creating new
- `_respawnPlayer` and `_exitToEditor` also fixed to remove ALL death screens
- Added `isDead = false` reset on respawn

### Interpreter Fixes
- `fog on`/`fog off` → new `setFog` intent (was falling through to fuzzy asset search → "Donkey")
- `particles fire` → mapped to `embers`, `particles rain` → triggers `setWeather('rain')`
- `clear weather` → proper `setWeather('clear')` (was triggering `clearScene`)
- Removed `fog` from WEATHER_TYPES (handled by dedicated setFog handler)
- `bridge.setWeather('clear')` was calling `execSingle('clear')` → scene clear. Fixed to call `setWeather(null)` directly

### Menu Command Test Results
- ✅ time morning/sunset/night, fog on/off, particles rain/snow/fire, all water presets, all weapon equips
- ⚠️ `add house` routes to `addInterior` in interpreter (might want separate "add house model" vs "interior house")
- ⚠️ `interior house/shop/tavern` may hang (heavy GLB loading)
- ⚠️ NPCs kill player in ~2 seconds — damage needs tuning

### Version State
- engine.mjs v117, character.mjs v60, index.html v117
- All pushed to GitHub main branch

### User Requests
- Kohari asked to switch from Opus 4.6 to Sonnet 4.6 (cost savings) — needs config change
- Kohari confirmed AI model already on free Gemini Flash (worker side) ✅
- Kohari wants all primitives gone — characters done, weapons now done (v117)

## Rust Engine Design Complete
- Wrote full `research/RUST-ENGINE-DESIGN.md` — 1,231 lines, Sections A-J
- H: Climbing & Ledge Grab — raycast-based ledge detection, grab/shimmy/pull-up/wall-climb states
- I: Update Order & Frame Scheduler — fixed timestep physics, variable animation/camera, strict order (input→AI→characters→physics→animation→IK→camera→render)
- J: Tooling/QA — project file layout, asset_validator CLI, bone_inspector CLI, 7 acceptance tests, 10 common failure modes, 6-phase implementation plan (16 weeks)

## v118 Session (evening)
- **Rust Engine Design complete** — wrote `research/RUST-ENGINE-DESIGN.md` (1,231 lines, Sections A-J)
  - H: Climbing & Ledge Grab (raycast ledge detection, grab/shimmy/pull-up/wall-climb/wall-jump)
  - I: Update Order & Frame Scheduler (fixed timestep physics, strict order, interpolation)
  - J: Tooling/QA (file layout, asset_validator, bone_inspector, 7 acceptance tests, 10 failure modes, 6-phase 16-week plan)
- **Fixed double NPC damage bug** — character.mjs was setting `player.health` directly AND engine.mjs called `takeDamage()`. Removed direct path, all damage through `takeDamage()` now
- **Reduced NPC damage** — 2-5 per hit (was 3-7)
- **Added 3s spawn invincibility** after respawn
- **Character State Machine** — `CharacterStateMachine` class added to character.mjs
  - 18 states, state locking, input buffering, auto-transitions, combo chains
  - Exported: `CharacterState`, `CharacterStateMachine`
  - Wired into CharacterController constructor (`this.stateMachine`)
  - NOT YET wired into movement/combat update loop — next step
- **Versions**: engine.mjs v118, character.mjs v63, index.html v=118
- Deployed to Cloudflare Pages, pushed to GitHub

## v124-v127 Session (continued)
- **v124**: Interior collision system — _getGroundY() checks terrain + solid objects, _checkWallCollision() for walls, wall sliding, step-up <0.4m, window._sceneObjects exposed
- **v125**: Shared animation system — KayKit chars without animations auto-load from knight GLB, retargets track names by stripping HumanArmature| prefix
- **v126**: Interactive doors — door mesh in createWallWithDoor(), pivot hinge, smooth open/close lerp, E key toggle, open doors remove isSolid
- **v127**: NPC AI State Machine — 8 states (IDLE/PATROL/CHASE/ATTACK/FLEE/DEAD/STUNNED/RETURN/SEARCH), alert level system, leash range, flee at 20% HP, search last known position
- **Versions**: engine.mjs v127, character.mjs v69, index.html v=127
- All pushed to GitHub and deployed to Cloudflare Pages

## v131-v135 Session (late evening)
- **v131**: Hit markers (red × hit, gold ✕ kill), kill feed (top-right), interaction prompts ([E] Open Door etc)
- **v132**: HUD overhaul — weapon slot indicators (bottom center), ammo counter, health color shift (green→yellow→red), state machine debug display
- **v133**: Compass bar (top of screen, rotates with camera), minimap NPC state colors (blue/red/orange/gray)
- **v134**: Sprint FOV (+12° sprinting, +5° running), dodge roll camera tilt
- **v135**: Screen shake system — intensity-based with decay, heavy/light/damage triggers
- **Versions**: engine.mjs v135, character.mjs v77, index.html v=135
- **Total session**: 18 versions (v118-v135)

## v132: AAA HUD Overhaul
- Replaced `createGameHUD()` with modern design — dark translucent panels, gradient bars, glow effects
- Health/Stamina/XP bars: bottom-left, labeled with values, low-health pulse animation
- Weapon quickslots: bottom-right, 3 slots with active gold highlight (Elden Ring style)
- Ammo counter: large text next to weapon slots for ranged weapons
- Compass: cleaner top-center with gold center marker
- Damage vignette: replaces old red flash div — uses CSS animation on inset box-shadow
- Controls hint: auto-fades after 6 seconds
- Interaction prompt: center-bottom for [E]/[F] prompts
- `window._hudUpdate` API: health(), stamina(), xp(), weapon(), ammo(), damageFlash(), interact()
- LevelSystem._createUI() now no-op (XP integrated into main HUD)
- LevelSystem._updateUI() calls `window._hudUpdate.xp()` 
- Removed old standalone hud-weapon-slots and hud-ammo elements
- Version: engine.mjs v132, character.mjs v74, index.html v=132
- Committed a3f1b80, pushed, deployed
