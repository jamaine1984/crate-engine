# 2026-03-01 — Crate Engine v228 Session

## What happened
- Continued building Crate Engine features from v226 → v228
- Engine now at ~23,267 lines, 430+ functions

## v227-228 Features Added
- **Rain puddles** — reflective ground puddles during rain (had `_getTerrainY` scope bug, fixed)
- **Weather command** — `weather rain/storm/snow/clear` aliases
- **Biome system** — `biome desert/snow/jungle/swamp/volcanic/arctic` changes ground color + particles + fog
- **Minimap** — circular radar minimap showing objects, NPCs, player direction
- **Inventory system** — pickup/grab/collect nearby items, `inventory` command
- **Quest log** — `quests` command (stub for NPC quest giving)
- **Water splash effects** — particle splash when entering water
- **Smart Traffic AI** — cars stop at intersections, stop for pedestrians/player, collision avoidance between cars, traffic light phases, GLB model cars (Kenney)
- **NPC sidewalk constraint** — NPCs walk on sidewalks, cross at crosswalks only
- **Building snap-off-road** — helper to keep buildings off road surfaces

## Bugs Fixed
- Tour popup moved to toolbar (no longer auto-pops on load)
- Quick-start scene selector no longer auto-shows
- Terrain/Landscape gallery no longer triggers on "generate" commands (requires explicit "show/browse" prefix)
- `createWaterSplash` duplicate declaration
- `dt is not defined` in setWeather (misplaced updateRainPuddles call)
- `_getTerrainY is not defined` in rain puddles (scope issue — added typeof guard)
- Bumped cache version to v=227 in index.html

## Key Bugs Still Open
- Biome ground color change not visually working (Ground mesh found but color may be overridden by texture)
- Red sports car (ferrari?) is too small — needs scale fix
- Ferrari should be drivable traffic car too
- Need more premium Sketchfab car/plane/helicopter downloads
- Kenney car models loaded at scale 1.2 — may need tuning

## Kohari's Requests (Still Working)
- Cars should be right size and driving
- Buildings NOT on roads
- NPCs ONLY on sidewalks/crosswalks
- Premium HD assets visible (nice cars, planes, helicopters flying)
- Cars stop at lights, stop for pedestrians, no passing through objects
- Keep recording data

## Architecture Notes
- `window._runCommand` is the way to execute commands programmatically
- Quick-start modal id: `quick-start-modal`, uses `_runCommand` on card click
- Tutorial auto-show now disabled (returns early)
- Smart traffic uses `window._trafficCars` array, updated in `updateSmartTraffic(dt)` called from animate loop
- NPC waypoint picking: `_pickWaypoint` in character.mjs NPCController update
- City template places buildings at ±25 from road center, roads at x=0 and cross streets at z=-40,40,120

## Session 2 — Late Night

### Critical Bug: MODEL_SCALE_OVERRIDES
- `loadGLBModel` line 6657: `const scaleOverride` name collision breaks ALL model loading
- Must rename the conflicting variable before anything visual works

### Car System Overhaul
- Replaced old `spawnDrivingCar` with `spawnTrafficCar` in city template
- Kenney car scale: 1.2→2.8
- `MODEL_SCALE_OVERRIDES` map added for per-model scaling
- `spawnTrafficCar` in city template setTimeout (line 12829) never fires — needs debugging

### Version Status
- v227 committed to git
- v228 patches applied but uncommitted (car system, weather, biomes, minimap, etc.)
- Cache bust at v=227 in index.html

### Next Priority Order
1. Fix scaleOverride variable conflict (blocks everything)
2. Debug why spawnTrafficCar never executes
3. Fix onDone undefined in GLB loading
4. Deploy + test visually
5. Commit v228

## Session 3 — City Layout Rewrite

### Fixed
- scaleOverride variable conflict in loadGLBModel (renamed to modelScaleOvr)
- Added missing _loadGLBFromUrl function
- Fixed modelFile/modelPath not defined in gltfLoader callbacks
- Fixed onDone not defined (typeof guard)
- Copied ph_*/hd_*/kenney_cars from Models/ to models/ (case-sensitive Cloudflare fix)
- Created Kenney colormap.png texture

### City Layout v4 (final)
- 800x800 ground plane
- 5x5 road grid (roads at x/z = -200, -100, 0, 100, 200)
- 16 city blocks in 4x4 grid
- DOWNTOWN (center 4 blocks): 4 skyscrapers + 4 apartments ONLY
- COMMERCIAL (ring of 8 blocks): shops + apartments
- RESIDENTIAL (4 corner blocks): houses with fences, pools, trees
- Houses rotated to face nearest road
- 25 traffic cars driving across all roads
- 7 NPCs (reduced from 28)
- Traffic lights at downtown intersections
- Stop signs at outer intersections
- Street lamps along main avenue
- 5 recording tabs running with autorecord=city

### Key: v237 committed, deployed at f34c20eb
