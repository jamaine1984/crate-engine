# 2026-03-02 — Crate Engine Debug Session

## Bug Found: `raw is not defined` at line 15750 (deployed v238)
- Deployed site is v238 but git repo only has v237 (285 extra lines in production)
- City command parsing lines (`add parking lot`, `add gas station`, `add sidewalk`, `add bridge`, `add street lamp`, `add dumpster`) got **misplaced inside `isVehicle()` function**
- `isVehicle()` doesn't have `raw` in scope — only receives a 3D object
- `isVehicle()` called every frame for vehicle proximity detection → error fires hundreds of times/sec
- This causes: page freezing, play mode janky, 3D models not loading, play button broken
- Fix: move those lines back into `execSingle()` where they belong

## Key Details
- Deployed engine.mjs: 23,737 lines
- Local engine.mjs: 23,452 lines  
- index.html on site: v=238, local: v=237
- Kohari made changes and deployed without committing to git

## Plan
- Pull deployed v238 code, fix misplaced lines, redeploy
- Then test everything with browser relay
